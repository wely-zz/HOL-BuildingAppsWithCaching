Before executing the end solution provided for this exercise, make sure to:
- Set the CloudShop.Azure cloud project as the startup project
- Replace the NorthwindEntities connection string in the Web.config file of the CloudShop project with the information of your Windows Azure SQL Database and Server